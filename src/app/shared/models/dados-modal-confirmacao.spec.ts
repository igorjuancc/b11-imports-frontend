import { DadosModalConfirmacao } from './dados-modal-confirmacao';

describe('DadosModalConfirmacao', () => {
  it('should create an instance', () => {
    expect(new DadosModalConfirmacao()).toBeTruthy();
  });
});
